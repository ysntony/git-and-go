interface:
  display_name: "Git and Go"
  short_description: "GitLab repo-first setup, commit, push, and MR coach"
  brand_color: "#16A34A"
  default_prompt: "Use $git-and-go to set up a GitLab repo if needed, repo-ize this work, review the diff, commit, push, and prepare MR context."

policy:
  allow_implicit_invocation: true
